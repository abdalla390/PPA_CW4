public class GameView {
    private Canvas gameCanvas;
    private GraphicsContext gc;
    private GameEngine gameEngine;
    
    public void render() {
        // Clear canvas
        // Draw background
        // Draw player, enemies, obstacles
        // Draw UI elements (hearts, score, etc.)
    }
    
    // Methods for specific rendering tasks
}